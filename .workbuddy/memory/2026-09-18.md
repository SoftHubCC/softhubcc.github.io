# 2026-09-18

- index.html 移动端阅读适配：新增 `@media (max-width: 768px)` 与 `@media (max-width: 400px)` 断点。核心修复：projects 网格 `minmax(480px,1fr)` 在窄屏强制 480px 造成横向溢出 → 移动端改为单列；Hero/卡片标题字号与内边距按移动端缩小；按钮铺满、min-height 44px 保证触控目标；body 加 `-webkit-text-size-adjust: 100%`。
